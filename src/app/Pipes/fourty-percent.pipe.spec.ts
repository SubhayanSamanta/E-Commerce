import { FourtyPercentPipe } from './fourty-percent.pipe';

describe('FourtyPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new FourtyPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
